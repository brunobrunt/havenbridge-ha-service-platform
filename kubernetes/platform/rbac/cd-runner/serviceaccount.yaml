#  creates the Kubernetes identity that the CD runner will eventually use. 
# It does not give that identity any permissions yet

apiVersion: v1
kind: ServiceAccount
metadata:
  name: havenbridge-deployer
  namespace: havenbridge
automountServiceAccountToken: false
